# Zaleznosci projektu HIGGS. Zalecany Python >= 3.10.
# Wersje minimalne (znane jako stabilne i kompatybilne z uzytym API).
# Po instalacji zapisz DOKLADNE wersje do raportu: pip freeze (sekcja "reprodukcja" w README.md).

numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3      # SGDClassifier(loss="log_loss") wymaga >= 1.1; 1.3 to bezpieczna baza

# Srodowisko notebooka
jupyterlab>=4.0
ipykernel>=6.0
